# OpenArm teleoperation package

This package contains the active MuJoCo, VIVE, inverse-kinematics, and guarded
physical-control implementation. Use the launchers under `scripts/launch/`
instead of executing these modules by file path; the launchers establish the
project root, Python module path, configuration, and safety arguments.
